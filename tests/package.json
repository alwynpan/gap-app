{
  "name": "gap-portal-e2e",
  "version": "1.0.0",
  "description": "G.A.P. Portal Playwright E2E Tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:allure": "playwright test && allure generate -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@testcontainers/postgresql": "^11.14.0",
    "allure": "^3.4.1",
    "allure-playwright": "^3.6.0",
    "bcryptjs": "^2.4.3",
    "pg": "^8.14.1"
  }
}
